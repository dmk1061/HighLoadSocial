spring:
  mvc:
    static-path-pattern: /public/**
  datasource:
    master:
      driver-class-name: org.postgresql.Driver
      url: jdbc:postgresql://citus_master_main:5432/postgres
      username: postgres
      password: postgres
  flyway:
    baseline-on-migrate: true
    enabled: true
    placeholders:
      secret-key: secret-key

secret-key: secret-key

tarantool:
  host: tarantool
  port: 3301
  username: admin
  password: admin

server:
  port: 8080

base:
  url: arch.homework
  dialog-host: localhost
  dialog-port: 8081
  counter-host: localhost
  counter-port: 8082

management:
  endpoints:
    web:
      exposure:
        include: [ "*" ]
  endpoint:
    prometheus:
      enabled: true